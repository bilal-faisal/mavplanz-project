import React from "react";

export const metadata = {
  title: "Mavplanz - Services",
  description: "Mavplanz services page",
};

const page = () => {
  return (
    <div>
      Services
    </div>
  );
};

export default page;
